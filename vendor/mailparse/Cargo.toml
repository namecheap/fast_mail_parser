# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2018"
name = "mailparse"
version = "0.16.1"
authors = ["Kartikaya Gupta"]
exclude = [
    ".gitattributes",
    ".gitignore",
    ".github/**",
    "examples/**",
]
description = "A simple parser for MIME e-mail messages"
homepage = "https://github.com/staktrace/mailparse/blob/master/README.md"
readme = "README.md"
keywords = [
    "parser",
    "email",
    "rfc822",
    "mime",
    "maildir",
]
categories = [
    "email",
    "parsing",
]
license = "0BSD"
repository = "https://github.com/staktrace/mailparse"

[dependencies.base64-simd]
version = "0.8"

[dependencies.memchr]
version = "2.7.0"

[dependencies.charset]
version = "0.1.3"

[dependencies.data-encoding]
version = "2.6.0"

[dependencies.quoted_printable]
# Pinned exactly, not `0.5.0`. 0.5.2 changed what Robust mode emits for a body
# whose last line ends in a soft break (`b"abc=\n"` -> `b"abc"`, where 0.5.1
# gives `b"abc\r\n"`), and `src/qp.rs` reproduces 0.5.1 because that is what this
# library ships. A caret requirement let `cargo update` change that behaviour --
# for encoded-word headers too -- with nothing in the suite to catch it, and it
# made this crate's own `cargo test` resolve a different version than the
# extension links, so the differential test was not testing what ships.
version = "=0.5.1"

[dev-dependencies.ouroboros]
version = "0.17.0"

[badges.maintenance]
status = "passively-maintained"
